let quote: string= "The most valuable of all talents is that of never using two words when one will do.";
let famous_person: string= "Thomas Jefferson";
let message=`${famous_person} once said, ${quote}`;
console.log(message);
